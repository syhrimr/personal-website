import blog from "./blog-schema";
import home from "./home-schema";
import project from "./project-schema";
import work from "./work-schema";

const schemas: any[] = [home, blog, work, project];

export default schemas;